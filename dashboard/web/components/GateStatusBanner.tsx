"use client";
/**
 * GateStatusBanner — RETIRED 2026-06-26.
 *
 * This banner reported whether the structural gating system was filtering
 * entries. The structural gates were removed with the scalp band (trend-only
 * operation), so there is nothing to report. It is no longer mounted anywhere;
 * kept as a null stub so any lingering import keeps compiling.
 */
export default function GateStatusBanner() {
  return null;
}
